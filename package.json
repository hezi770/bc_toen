{
  "name": "demo-site",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "npm run -w @demo/server start",
    "dev:server": "npm run -w @demo/server dev",
    "dev:client": "npm run -w @demo/client dev",
    "build": "npm run -w @demo/client build"
  }
}
